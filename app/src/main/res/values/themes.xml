<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The Compose tree paints everything; this just keeps the window dark
         before the first frame so the app does not flash white on launch. -->
    <style name="Theme.PointEast" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0B0E11</item>
        <item name="android:statusBarColor">#0B0E11</item>
        <item name="android:navigationBarColor">#0B0E11</item>
    </style>
</resources>
